{
  "name": "todo",
  "version": "1.0.0",
  "description": "server for marand todo",
  "main": "index.js",
  "dependencies": {
    "body-parser": "^1.18.1",
    "express": "^4.15.4",
    "nodejs": "0.0.0"
  },
  "devDependencies": {
    "@types/body-parser": "^1.16.5",
    "@types/express": "*",
    "@types/node": "^8.0.28",
    "typescript": "^2.6.0"
  },
  "scripts": {
    "b": "tsc && (mkdir dist || true) && mv src/*.js* dist/ && npm run build --prefix frontend && cp -r frontend/dist/* dist/",
    "prod": "tsc && (mkdir dist || true) && mv src/*.js* dist/ && npm run build-prod-ngsw --prefix frontend && cp -r frontend/dist/* dist/",
    "p": "npm run prod && (rm dist/data.json || true) && scp -r dist/* pi@marand.dk:/home/pi/code/todo",
    "start": "node dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MyrionSC/todo.git"
  },
  "author": "Marand",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/MyrionSC/todo/issues"
  },
  "homepage": "https://github.com/MyrionSC/todo#readme"
}
